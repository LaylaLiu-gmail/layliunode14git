trigger:
- none

pool:
  vmImage: 'windows-latest'

variables:
  GITHUB_USERNAME: 'LaylaLiu-gmail'
  GITHUB_REPO: 'testmigration'    # The GitHub repository name (e.g., 'my-repo')

steps:
- script: |
    git config --global user.name 'azure-devops'
    git config --global user.email 'azure-devops@devops.com'

    cd $(Build.SourcesDirectory)
    git log
    git remote add github https://$(GITHUB_TOKEN)@github.com/LaylaLiu-gmail/testmigration.git
    git remote -v
    git push --mirror github
    git branch --all
    git push github --tags
    git branch --all
  displayName: 'Push to GitHub'