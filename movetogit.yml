trigger:
- none

pool:
  vmImage: 'windows-latest'

variables:
  GITHUB_USERNAME: 'LaylaLiu-gmail'
  GITHUB_REPO: 'layliunode14git'    # The GitHub repository name (e.g., 'my-repo')

steps:
- script: |
    git config --global user.name 'azure-devops'
    git config --global user.email 'azure-devops@devops.com'

    cd $(Build.SourcesDirectory)
    echo "aa"
    dir
    git remote add github https://$(GITHUB_TOKEN)@github.com/LaylaLiu-gmail/layliunode14git.git
    git remove -v
    git push github --all
    echo "cc"
    git push github --tags
  displayName: 'Push to GitHub'